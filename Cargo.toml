[package]
name = "runx"
version = "0.1.0"
edition = "2021"
description = "Universal CLI for task orchestration with intelligent watch"

[dependencies]
clap = { version = "4", features = ["derive"] }
toml = "0.8"
serde = { version = "1", features = ["derive"] }
notify = "6"
glob = "0.3"
colored = "2"
anyhow = "1"
