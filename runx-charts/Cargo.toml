[package]
name = "runx-charts"
version = "0.1.0"
edition = "2021"
description = "Chart templates for Runx test visualization"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
